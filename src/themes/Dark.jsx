import { createTheme } from "@mui/material";

const Dark = createTheme({
  palette: {
    mode: "dark",
    primary: {
      main: "#00897b",
    },
    secondary: {
      main: "#43a047",
    },
  },
});

export default Dark;
